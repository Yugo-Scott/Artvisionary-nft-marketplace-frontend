/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
}

// module.exports = nextConfig

// module.exports = {
//   images: {
//     domains: ["localhost"], // ここに画像をホストしているドメインを追加します
//   },
// };